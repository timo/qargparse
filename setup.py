from setuptools import setup, find_packages

setup(
    name="qargparse",
    version="0.0.1",
    description="automatically generate QDialogs from argparse objects",
    packages = find_packages(),
    )

